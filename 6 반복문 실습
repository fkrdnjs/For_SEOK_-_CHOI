//1
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int n;
    int i=1;
    
    scanf("%d",&n);
    
    while(1){
        if(n%i == 0 && i != n)
            printf("%d ",i);
        if(i == n)
            printf("%d",i);
        i += 1;
        if(i > n)
            break;
        
    }
	return 0;
}

//2
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int n;
    int total = 0;
    
    while(1){
        scanf("%d",&n);
        
        if( n == 0)
            break;
            
        total += n;
    }
    
    printf("%d",total);
    
	return 0;
}
