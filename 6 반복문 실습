//1
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int n;
    int i=1;
    
    scanf("%d",&n);
    
    while(1){
        if(n%i == 0 && i != n)
            printf("%d ",i);
        if(i == n)
            printf("%d",i);
        i += 1;
        if(i > n)
            break;
        
    }
	return 0;
}

//2

