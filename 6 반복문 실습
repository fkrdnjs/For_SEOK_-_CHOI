//1
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int n;
    int i;
    
    scanf("%d",&n);
    
    for(i=1;i<=n;i++){
        if(n%i == 0 && i != n)
            printf("%d ",i);
        if(i == n)
            printf("%d",i);
    }

	return 0;
}
