//3번

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
	int num[5];
	int i;
	int max1, max2;
	int k1 = 0;
	
	for(i=0;i<5;i++)
	    scanf("%d",&num[i]);
	
	max1 = num[0];
	max2 = num[0];
	
	for(i=0;i<5;i++){
	    if(max1 <= num[i]){
	        max1 = num[i];
	        k1 = i;
	    }
	}
	
	if(k1 == 0){
	    max2 = num[1];
	    for(i=1;i<5;i++){
	        if(max2 <= num[i])
	            max2 = num[i];
	    }
	}
	
	else{
	    for(i=0;i<5;i++){
	        if(max2 <= num[i] && i != k1)
	            max2 = num[i];
	    }
	}
	
	printf("%d\n%d",max1, max2);
	
	return 0;
}
