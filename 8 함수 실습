//1
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int add(int x, int y);

int main()
{
	int n;
	int i;
	int result = 0;
	
	scanf("%d",&n);
	
	for(i=1;i<=n;i++)
	    result = add(result, i);
	
    printf("%d",result);
    
	return 0;
}

int add(int x, int y){
    return x + y;
}
